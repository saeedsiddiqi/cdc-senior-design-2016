
# Initialization of libraries, must source this first
library(readxl)
library(EpiModel)
library(ggplot2)
library(reshape2)
library(plotly)
# library(shinydashboard)

## For installation
pkgs <- c("readxl", "EpiModel", "ggplot2",
              "reshape2", "plotly", "shinydashboard")

# Uncomment below to install
# install.packages(pkgs)